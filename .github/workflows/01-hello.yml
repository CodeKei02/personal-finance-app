name: 01 - Hello
on:
  push:
    branches: [main]
jobs:
  hello:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Saludar
        run: echo "Hola desde Github Actions"
      - name: Ver archivos disponibles x2
        run: ls -la
